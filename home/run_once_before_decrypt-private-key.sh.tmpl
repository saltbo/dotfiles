#!/bin/sh

if [ ! -f "/Users/saltbo/.config/chezmoi/key.txt" ]; then
    mkdir -p "/Users/saltbo/.config/chezmoi"
    chezmoi age decrypt --output "/Users/saltbo/.config/chezmoi/key.txt" --passphrase "{{ .chezmoi.sourceDir }}/key.txt.age"
    chmod 600 "/Users/saltbo/.config/chezmoi/key.txt"
fi
